<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wasteless">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Wasteless">
        <activity android:name=".ui.DetailActivity"
            android:label="Order Detail"/>
        <activity android:name=".ui.stakeholder.ScanQRActivity"
            android:label="Scan Order QR"
            android:parentActivityName=".ui.stakeholder.StakeholderActivity"/>
        <activity android:name=".ui.user.AddOrderActivity"
            android:label="Create Order"
            android:parentActivityName=".ui.user.UserActivity"/>
        <activity android:name=".ui.stakeholder.StakeholderActivity" />
        <activity android:name=".ui.user.UserActivity" />
        <activity android:name=".ui.RegisterActivity"
            android:label="Register"
            android:parentActivityName=".ui.LoginActivity"/>
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>